version: 2

sources:
  - name: raw
    database: "project-b8fc8724-8adc-4499-9a4"
    schema: raw
    tables:
      - name: raw_google_ads
        columns:
          - name: google_event_id
            data_tests: [not_null]

      - name: raw_meta_ads
        columns:
          - name: meta_event_id
            data_tests: [not_null]

      - name: raw_tiktok_ads
        columns:
          - name: tiktok_event_id
            data_tests: [not_null]

      - name: raw_email_events
        columns:
          - name: email_event_id
            data_tests: [not_null]

      - name: raw_web_events
        columns:
          - name: event_id
            data_tests: [not_null]

      - name: raw_conversions
        columns:
          - name: conversion_id
            data_tests: [not_null, unique]
          - name: user_id
            data_tests: [not_null]

      - name: raw_ad_spend
        columns:
          - name: spend_id
            data_tests: [not_null]

      - name: raw_user_identity
        columns:
          - name: user_id
            data_tests: [not_null, unique]